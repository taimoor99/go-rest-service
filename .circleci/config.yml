version: 2
jobs:
  build:
    docker:
      - image: pdacosta/circle-golang-gcloud:0.0.5

    working_directory: /go/src/github.com/pedrocelso/go-rest-service
    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: go test -v lib/services/user/users_test.go
      - run: go test -v lib/services/task/tasks_test.go


